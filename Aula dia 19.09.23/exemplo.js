// Esttrutura de Repetição

const array = ['X-tudo', 'X-Egg-Baccon', 'Coxinha', 'Pizza de queijo', 'Esfiha']; //7

for (let index = -0; index < array.length; index++) {
    const elemento = array[index];
    console.log(index, elemento)
}